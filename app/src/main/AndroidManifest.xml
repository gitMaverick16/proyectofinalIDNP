<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectoidnp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.entrenamiento.entrenamiento" />
        <activity android:name=".view.reproductor.reproductor" />
        <activity android:name=".view.registro.RegisterView" />
        <activity android:name=".view.perfil.perfil" />
        <activity android:name=".view.historial.historial" />
        <activity android:name=".view.estadisticas.estadisticas" />
        <activity android:name=".view.autenticacion.autenticacion" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>